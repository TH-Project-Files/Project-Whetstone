node_modules/
.DS_Store
*.local.*

# Real campaign outputs are generated, not committed. The template and the worked example
# (regenerable via `node runner/gristmill.workflow.js`) are kept as reference.
workspace/*/
!workspace/_TEMPLATE/
!workspace/example-campaign/

# Side-review / campaign scratch — never commit to the public kit
KIT_SOURCE.md
